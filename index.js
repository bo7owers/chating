$(document).ready(function () {
    let $man = $('.persona1');
    let $woman = $('.persona2');
    let $nxtBtn = $('#nextBtn');
    let $dialogue = $('.dialogue');

    // dialogues 
    let dialogues = {
        man: "Man",
        phrase1: "Hello there"
    }
    
    $($nxtBtn).on('click', () => {

    })






});